# Token Optimizer Example Project

Token Optimizer is local-first context hygiene tooling for AI coding sessions.
It helps a coding agent avoid repeated full-file reads by using explicit
summaries, outlines, and static audits.

The project starts with a Python CLI and should remain inert unless the user
invokes explicit commands.
